<!doctype html>
<html lang="en">
    <head>
        <title>Node Flashcard App</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <link href="/styles/nav.css" rel="stylesheet" type="text/css">
        <link href="/styles/communityDecks.css" rel="stylesheet" type="text/css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">
    <head>
    <body>
        <% include partials/nav.ejs %>
        <h1 id="title">Community Flashcard Decks<h1>
        <br>
        <div class="container">
            <table class="table table-sm">
                <thead>
                    <th>Deck Name</th>
                    <th>Creator</th>
                    <th>Download</th> 
                </thead>
                <% for (let i=0; i < data.length; i++){ %>
                    <% for (let j=0; j < data[i].decks.length; j++){ %>
                    <tr>
                        <td><div><a href=<%= "/communityDecks/"+ data[i].username + "/" + data[i].decks[j].deckName  %>><%= data[i].decks[j].deckName %></a></div></td>
                        <td><%= data[i].username %></td>
                        <td><button type="button" class="fas fa-download" aria-label="Download"><span aria-hidden="true"></span></button></td>
                    </tr>
                        <% } %>
                    <% } %>
            </table>
        </div>    
        <script src="/ajax/communityDecks.js" type="text/javascript"></script>
    <body>
<html>